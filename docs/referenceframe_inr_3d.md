# Reference-Frame 分区 INR 压缩 — 3D 扩展（deltaWing）

> 启动:2026-07-20,用户指令"现在进一步开始做 3d",数据
> `flowData3D/deltaWing_mag0_3reesampled.nc`。
> 2D 项目(方法、协议、全部教训)见 [referenceframe_inr_v2.md](referenceframe_inr_v2.md)
> (已收官:三场九格记分板全绿);本文档只记录 3D 新增部分,2D 的规则默认沿用
> (epochs ≤2000、种子 ≤3 {0,7777,1}、M ≤3、字节口径 = 参数×4+边信息 ≤ frac×场字节、
> 结论可追溯不许静默翻转)。

## 0. 数据

`deltaWing_mag0_3reesampled.nc`:非定常 3D 向量场,变量 u/v/w + 坐标 x/y/z/t。
维度 (T, Z, Y, X) = (171, 55, 314, 55),float32,全量 162,424,350 点 × 3 分量
× 4 B ≈ **1.81 GiB**(约为 2D cylinder 的 50 倍样本数)。物理:三角翼绕流
(前缘涡为主结构)。

## 1. 代码(`experiments/referenceframe_inr_3d/`)

v2 的维度直推,**v2 目录保持冻结**,3D 代码单独成目录;INR 训练器/归一化/
架构注册表(coordnet=SIREN / mlp / finer)直接 `import` 自 v2 的 `inr.py`
(其本身维度通用:k = coords.shape[1], p = values.shape[1],参数闭式公式
`coordnet_num_params(m, d, k, p)` 对 k=4,p=3 成立且有 torch 断言)。

| 模块 | 内容 | 与 2D 的差异 |
|---|---|---|
| `killing3d.py` | 6-DOF Killing LSQ(cell 充分统计量):u(x) = t_vec + w×x,A 列 = [J e_i \| J(e_i×x) − e_i×v] | 2D 是它的 wz 切片(e_z×x = x⊥);cell 变 k³ 体素;AtA (T,nCz,nCy,nCx,6,6),内存 ≈ T·nCells·288 B(k_cell 默认 4) |
| `frame3d.py` | 观察系积分 + exact pullback + 逆变换:dR/dt = ŵR 逐步 Rodrigues 累乘(非交换),D = ∫Rᵀt_vec ds 梯形;xi = Rᵀx − D,vtil = Rᵀ(v−u),v = R vtil + u | 2D 的 θ 累加是 w=(0,0,c) 特例(T5 逐位锚定);推导 D 的方程:x = Rξ+b ⇒ t_vec = ḃ−w×b,D:=Rᵀb ⇒ Ḋ = Rᵀt_vec |
| `partition3d.py` | τ-merge(残差比准则不变) | cell 6-邻接;标签 3D 展开;absorb 单位 = 体素 |
| `synth3d.py` | 合成场:斜轴旋转相机(闭式解 w = −ω0 n, t_vec = c0×w)、平移场、双转子(异轴异速) | 闭式解是 2D true_killing_params 的 n=e_z 推广 |
| `validate_rft3d.py` | T1-T5 验证套件(见 §2) | 新增 T5 = 2D 嵌入锚点 |
| `pipeline3d.py` | 数据加载(netCDF4 直读 + stride 降采样)、预算、observer 变体(tvfull/tvtrans/constfull/consttrans,6/3 DOF)、字节口径、训练/重建/PSNR | coords (x,y,z,tn) k=4 p=3;边信息:bbox 6+6 浮点、observer 24/12 B(const 变体);新增 `max_steps_per_epoch`(见 §3 规模协议) |
| `run_experiment3d.py` | CLI(默认 n_windows=1 允许单窗 = 2D 赢点结构) | `--stride_t/--stride_xyz/--t_max` 数据降采样并打进 field 名 |

## 2. 验证套件(validate_rft3d.py,训练实验前必须全过)

- **T1** 斜轴旋转相机制造场(轴 n=(0.3,0.5,0.8) 归一、c0 偏心):killing 解
  收敛到闭式 (t_vec, w);E/E0→0;exact/solved q 的 pushforward 对所有 t 恒为
  steady 图案 s(xi)。
- **T2** 平移场(Taylor 冻结):consttrans/constfull 恢复 c_vec、E/E0≪1;
  反例 = 旋转场上纯平移必须失败(E/E0≈1)。
- **T3** oracle 往返:随机场+随机 6-DOF q+散乱 mask,pushforward→逆变换
  逐位还原(<1e-10)。
- **T4** 双转子反例(左绕 e_z ω=0.8、右绕 e_x ω=−1.4,轴和速率都不同):
  τ-merge 不许并成 N=1,每半 w 恢复真值。
- **T5** **2D 嵌入锚点**(本套件的核心保险):z-复制 2D 场(vz=0,zs 对称)
  下,killing3d 的 (tx,ty,wz) 必须 == killing2d 的 (a,b,c)(<1e-6)、面外
  DOF≈0、E/E0 与 2D 一致(<1e-9);integrate_frame_3d(wz-only) 与 2D θ/D
  逐位一致(<1e-9);单 z 层 pullback 样本 (xi, vtil) 与 2D 逐分量一致。
  ⇒ 3D 实现被钉死到已双机验证的 2D 冻结实现上。

结果:(待填,首跑进行中)

## 3. 规模协议(3D 专属,防止 2D recipe 直搬爆算力)

全量场 162M 样本/窗,v2 recipe 的"每 epoch 全扫"= 每 epoch ~5000 步
(batch 32k),1000ep = 5M 步,不可行(2D cylinder 是 ~100 步/epoch)。
新协议旋钮 `--max_steps_per_epoch S`(默认 0 = 关):

- S>0 时训练集 = 网格的固定随机子采样(S×batch_size 个样本,种子可控),
  **评估始终全网格**;epoch 含义变为"固定 S 步",与 v2 的
  min_steps_per_epoch(下限)对称地设上限。
- 使用它的运行必须在结果里注明 S(cfg 全量落盘,自动可追溯);与不带 cap 的
  数字不混排。
- 数据侧降采样(--stride_t/--stride_xyz)是另一条路:场名自动带 `_s{t}x{xyz}`
  后缀,视为**独立数据集变体**,禁止跨 stride 混排数字。

## 4. 实验记录

### 4.1 Verify_rft3d_1.1:验证套件 + deltaWing 干跑/冒烟(2026-07-20,本地)

**验证套件:17/17 全过**(第三轮;前两轮的修复过程如实记录):

| 轮 | 结果 | 修复 |
|---|---|---|
| 1 | 进程内存涨到 59.8 GB 被杀 | ①`region_solve_3d` 高级索引物化 (Tw, 64000, 6, 6) ≈ 12 GB 临时数组 → 全网格轴向 reduce + 分块累加;②merge 的 payload cache 过期条目不清理(3D 每条 ~6 KB × 10⁵ 条)→ 移除 cache,中选对 pop 后重算一次 |
| 2 | 16/17(T4 耗时 7 小时) | ③merge 候选对逐对小批量 solve 的 numpy 调度开销 → K 对堆叠单次批量 solve(~30×);④T4 cell 2³→3³;⑤T1 solved-q pushforward 检查移到细档(粗档 2.5e-2 参数误差 × 2π 时长的线性漂移 ≈ 0.31 属离散化收敛行为,细档 6e-3 → 漂移 0.085) |
| 3 | **17/17 PASS**(T4 从 7h → 41 min) | — |

关键读数:T3 往返 3.55e-15(机器精度,RFT 无损性质 3D 成立);**T5 锚点:killing3d
与 2D 解一致 5.14e-13、E/E0 完全相同、frame/采样一致 1e-15~1e-16**;T4 异轴双
转子拒并且每半 w 恢复(0.818/1.371 vs 0.8/1.4)。

**deltaWing 干跑诊断(diag_deltawing.py,s2x2 降采样档,(86,28,157,28))**:

| observer 变体 | E/E0 |
|---|---|
| tvfull | 0.6970(mean t=(0.000, **+1.141**, −0.137),w≈0) |
| tvtrans | 0.6982 |
| constfull | 0.7140 |
| consttrans | 0.7151(t=(0.000, +1.184, −0.195)) |

- 解出的全局平流 = y 向(来流方向,|t|≈1.19 vs mean|v|=2.50),物理合理;
  时变/旋转自由度几乎不加分(±0.02)⇒ 若用,const 变体足够。
- **E/E0≈0.70-0.72:单全局刚体观察者只解释 ~30% 时间能量**。对照 2D 判据序
  (rfc 3e-4 ≪ cylinder 0.114 < bouss 0.455):**deltaWing 是判据下的弱适用场**
  ——前缘涡三维脱落/破碎不是刚体平流,与 cylinder 的"冻结涡街"本质不同。
- τ-merge 局部同样不认:单 cell E/E0 0.77-0.90,τ=0.05/0.1 几乎不合并
  (N≈1900/1960 cells),τ=0.2 才出 3632 voxel 区域 ⇒ 分区结构对此场无增益
  预期,**合理结构 = M=1 单窗单区域**(tau=-1 快路径,15 s vs 全合并 ~20 min)。
- 注意:此为 s2x2 降采样读数;全量分辨率的 E/E0 待 Ibex 复核(差分更准,预期
  同量级)。

**训练冒烟(本地,s2x2 + 5% + mlp@1e-3 + 8ep×32 步,73 s 全链路)**:
baseline m=66(反解自预算)41.85 dB / pro-M1-consttrans 42.15 dB,字节
6,325,3xx ≤ 6,351,341 双侧断言过,CR=20.1×,边信息 63 B,重建无洞。
（8ep 欠训练数字,只证管线,不作结论。）

**读数与下一步**:E/E0 判据预测 deltaWing 上 RFT 增益小/平手——这本身是
"判据 3D 外推是否成立"的检验数据点(2D 故事的核心判据推广);若要 3D 正例
(可刚体解释场)需另配数据集。**用户 2026-07-20 裁定:"直接部署 A"**(deltaWing
记分板,s2x2 档先行)。

### 4.2 Verify_rft3d_1.1 wave-1:deltaWing s2x2 5% 记分板(Ibex job 49161181,部署 2026-07-20,结果待回收)

**任务表(18)**:{bl, ctM1} × mlp × lr {5e-4, 1e-3, 2e-3} × 3 种子 {0,7777,1} @
deltawing_s2x2 5%。脚本 `ibex_bash/refframe_3d_v1.sh`(commit 3d7f6f2)。

- **结构**:ctM1 = 单窗单区域 consttrans(tau=-1 快路径;E/E0 干跑显示分区/
  时变/旋转 DOF 均不加分,ct 边信息最小 63 B,与 bl 等宽 m=66);
- **lr probe 对称**:2D 教训 lr 是最大杠杆且 mlp 阶梯在 cylinder 到 1.5-2e-3
  才见顶,3D 场(样本 50 倍、m=66)lr* 未知 ⇒ {5e-4, 1e-3, 2e-3} 双臂对称;
- **步数协议**:max_steps_per_epoch=64 × 1000ep = 64k 步(与 2D recipe 同量级;
  s2x2 全扫 331 步/epoch 会 5 倍超时);
- **预期与判定口径**:E/E0=0.70 ⇒ 预期小增益/平手;记分板标准沿用 2D
  ("proposed 不得比同架构 baseline 更差",bouss 5% 的 +0.002 平手先例适用);
  无论正负,与 E/E0 判据的一致性单独记录(判据 3D 外推结论);
- 部署前核查:Ibex HEAD=3d7f6f2 一致、deltaWing s2x2 在 Ibex 加载 shape 一致、
  脚本 LF。留用额度:cf 臂 / 2.5%/10% 档 / 全量档,等 wave-1 读数定向投放。

**Wave-1 结果(2026-07-21 回收,18/18 零失败,~48 min/任务;3-seed 均值
[min..max],出处 job 49161181)**:

| lr | bl | ctM1 | Δ均值 | 种子配对(s0/s7777/s1) |
|---|---|---|---|---|
| 5e-4 | **52.14 [52.02..52.22]** | **52.17 [52.14..52.24]** | **+0.03** | +0.02 / −0.05 / +0.12(2/3 正) |
| 1e-3 | 52.05 [52.03..52.10] | 52.03 [51.93..52.09] | −0.02 | +0.04 / +0.06 / −0.17(2/3 正) |
| 2e-3 | 51.61 [51.33..51.79] | 51.42 [51.30..51.64] | −0.19 | −0.03 / −0.38 / −0.15(0/3) |

**读数**:
1. **lr 阶梯方向与 2D cylinder 相反**:3D deltaWing 上 lr 升高单调变差
   (bl 52.14 → 52.05 → 51.61),5e-4 是当前峰但**低端未探**(3e-4 可能更高;
   2D 的"mlp 高 lr 阶梯"不可跨维度外推——网宽 m=66、64k 步、数据平滑度都不同)。
2. **最优对最优:ctM1 52.17 vs bl 52.14 = +0.03,统计平手**("不更差"成立,
   与 bouss 5% 的 +0.002 同级);@5e-4 配对 2/3 正,pro 种子散布 0.10 < bl 0.20
   (observed field 训练更稳的 2D 发现在 3D 复现)。
3. **E/E0 判据的 3D 外推:成立**。判据预测 0.70 → 小增益/平手,实测 +0.03。
   跨维度判据-增益序列(各场最优对最优):rfc 0.0003→+7~12 ≫ cyl 0.114→+0.57
   > bouss 0.455→+0.002~+0.75 ≫ **deltawing 0.70→+0.03(平手)**——单调端点
   吻合(cyl/bouss 中段的次序交叉是 2D 已知现象)。
4. 高 lr 下 pro 转小劣(2e-3 配对 0/3,−0.19):与 2D 相反;ct 只移除 ~30%
   时间能量,observed field 与原场差别有限,收益天花板本来就低,高 lr 噪声
   放大后小成本显形。机理不追(判据已解释大局)。

**5% 档判定:deltawing×mlp 统计平手 +0.03,记分板"不更差"成立**;主要科学
结论 = E/E0 先验判据跨 2D/3D 成立,deltaWing 按判据即是弱适用场,实验证实。

**Wave-2(用户 2026-07-21 裁定"1 和 2 都部署,排队没问题可多铺";Ibex job
49221282,36 任务,脚本 `refframe_3d_v1b.sh`,commit 340a068,结果待回收)**:
既然排队无压力,按"一波钉死、不留第三波依赖"扩展:
- **A 段(12)**:5% 档 lr 低探 {bl,ctM1} × {3e-4, **2e-4**} × 3 种子——wave-1
  峰在 5e-4 且低端未探,两档下探防 3e-4 仍在升;
- **B 段(24)**:{bl,ctM1} × {2.5%, 10%} × lr {5e-4, **3e-4**} × 3 种子——档位
  铺开同时带两个候选峰 lr,峰若移到 3e-4 无需重跑档位。
cf 臂省略(干跑 E/E0 与 ct 无差 + 2D"无全局旋转场 ct≥cf"经验)。宽度反解:
2.5% → m=46、10% → m=93(s2x2 场字节 127 MiB 基准)。

**Wave-2 结果(2026-07-21 回收,36/36 零失败;2.5% 档 ~29 min、10% 档 ~70 min
/任务;3-seed 均值 [min..max],出处 job 49221282;5e-4 行 = wave-1 数据并列)**:

5% 档 lr 阶梯补全(A 段):

| lr @5% | bl | ctM1 |
|---|---|---|
| 2e-4 | 51.93 [51.85..52.05] | 51.83 [51.56..52.06] |
| 3e-4 | 52.03 [51.93..52.19] | 51.90 [51.81..51.97] |
| **5e-4**(w1) | **52.14** | **52.17** |
| 1e-3(w1) | 52.05 | 52.03 |
| 2e-3(w1) | 51.61 | 51.42 |

→ **lr 峰确认 = 5e-4**(单峰,双臂一致;低探两档单调更差)。

档位记分板(B 段;各档最优对最优):

| frac | bl(峰 lr) | ctM1(峰 lr) | Δ最优对最优 |
|---|---|---|---|
| 2.5% | 51.82 [51.59..52.00] @5e-4 | 51.85 [51.55..52.09] @5e-4 | **+0.03** |
| 5% | 52.14 @5e-4 | 52.17 @5e-4 | **+0.03** |
| 10% | 52.49 [52.39..52.60] @**3e-4** | 52.45 [52.31..52.54] @**3e-4** | **−0.04** |

**读数**:
1. **s2x2 记分板收官:三档全部统计平手**(+0.03 / +0.03 / −0.04,幅度全部
   远小于种子散布 0.1-0.5)——"不更差"三档成立(10% 的 −0.04 按 2D cylinder
   −0.20 判平手的先例,更是噪声内)。E/E0=0.70 判据的预测在三个预算档全兑现。
2. **10% 档 lr 峰移到 3e-4**(bl 3e-4 52.49 > 5e-4 52.44;ct 同向)——lr 峰
   随预算/网宽(m=93)移动;wave-2 的双 lr 设计正好接住,免了重跑。
3. **deltaWing 的 rate-distortion 曲线异常平坦**:预算翻倍只 +0.3 dB
   (51.85 → 52.17 → 52.49;对照 2D cylinder×mlp 是 +2 dB/倍)。mlp 在此场
   的重建质量早早卡在架构表达力(谱偏置)而非容量上 ⇒ RFT 搬参考系省下的
   有效复杂度换不成 PSNR——这是"平手"的机理注脚,与 E/E0 判据互补。
4. ctM1 在 2.5%@3e-4 出现过唯一一个 +0.13(51.72 vs 51.59),但双方峰值都在
   5e-4,最优对最优口径下不算数(按 2D 协议:最优对最优才公平)。

**Verify_rft3d_1.1 判定(s2x2 档,54 任务)**:deltawing×mlp 2.5/5/10% 三档
统计平手,"proposed 不更差"成立;**主结论 = E/E0 先验判据跨维度成立**
(0.70 → 三档平手,预测完全兑现),deltaWing 定性为 3D 弱适用场(前缘涡
三维破碎不可刚体解释 + mlp 容量早饱和双重机理)。s2x2 档收官;
全量分辨率复核与 SIREN(coordnet)侧补齐待用户裁定。
